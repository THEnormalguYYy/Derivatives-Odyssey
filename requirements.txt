# Derivatives Odyssey — Python dependencies
# Tested with Python 3.11. Versions below are the ones the project was
# validated against; pin exactly (==) for fully reproducible environments.

# --- Core numerics & data ---
numpy>=2.2
pandas>=2.2
scipy>=1.13

# --- Machine learning ---
scikit-learn>=1.5

# --- Econometric volatility models (GARCH baseline) ---
arch>=7.0
statsmodels>=0.14

# --- Visualization (notebooks / research) ---
matplotlib>=3.8
